{% extends 'base.html.twig' %}

{% block body %}
    <h1>Admin</h1>

    <a class="btn btn-primary" href="{{ path('admin_player_index') }}">Manage Players</a>

    <a class="btn btn-primary" href="{{ path('game_index') }}">Manage Games</a>


{% endblock %}